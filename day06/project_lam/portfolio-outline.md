# LAM 9600 식각 장비 이상탐지 — 제출용 포트폴리오 슬라이드 구성안

- 출처: `project_lam.ipynb` / `Personal_Notes.pdf` / `guide/lam_presentation_guide.html` / `result/` 폴더 CSV
- 규칙: 수치는 노트북 출력에 실제로 나온 값만 사용. 폴더에 없는 내용은 넣지 않음.
- 총 10장. 각 장 = ① 제목(한 줄) / ② 슬라이드 텍스트(불릿 3줄 이내) / ③ 시각적 요소(표·그림·두 개 비교·순서 흐름 중 하나).

---

## 1장. LAM 9600 식각 장비 센서 이상탐지 (FDC)

**슬라이드 텍스트**
- 목적: 식각 중 장비가 남긴 센서 기록만으로 정상 공정에서 벗어난 웨이퍼를 골라낼 수 있는지, 그리고 어떤 방법이 가장 나은지 찾기
- 데이터: LAM TCP 9600 금속식각 공개 벤치마크 — 12,829행 × 23열, 웨이퍼 129개, 식각 Step 4·5 구간
- 결론 예고: 시점이 아니라 웨이퍼 단위로 봐야 하고, 신호는 값의 '평균'이 아니라 '산포'에 있었다

**시각적 요소 — 순서 흐름**
문제 정의 → 전처리(이상값 교정·스텝 분리) → 분석 단위 정하기 → 모델·관리도 → 결론·전략

---

## 2장. 이 데이터의 정체

**슬라이드 텍스트**
- 정상 웨이퍼 108개 + 공정 파라미터를 일부러 규격에서 틀어놓은 웨이퍼 21개를 합쳐 `result`에 Pass/Fail을 붙인 것
- 그래서 `result=Fail`은 계측·전기검사 불합격이 아니라 '통제된 공정 이탈' — 제품 불량 예측이 아닌 FDC(공정 이상탐지) 문제
- 불균형 데이터: 행 기준 Fail 2,059건 / 16.05% (Pass 10,770 / 83.95%)

**시각적 요소 — 표**

| 항목 | 내용 |
|---|---|
| 크기 | 12,829행 × 23열 (1행 = 웨이퍼 한 장의 한 시점) |
| 식별 열 | wafer_id · Time · Step Number |
| 센서 열 | 19개 (BCl3·Cl2 유량, 압력, He 척압력, RF·TCP 파워/반사파워/매칭위치/임피던스/위상오차, Endpt A, Vat Valve) |
| 타깃 | result = Pass / Fail (웨이퍼 단위 라벨) |
| Fail 비율 | 행 기준 16.05% |

---

## 3장. 전처리 ① 물리적으로 불가능한 값 교정

**슬라이드 텍스트**
- 결측치·상수열 확인 결과 없음
- TCP 반사전력(`TCP Rfl Pwr`)에 음수(-1)가 1건 — 반사전력은 발생기로 되돌아온 전력이라 음수 불가, 계측 노이즈로 판단
- 해당 1건(l3321.txm, Time 38.7656)을 0으로 교정하고 원본은 그대로 둔 채 `lam_etch_modify_2.csv`로 새로 저장

**시각적 요소 — 두 개 비교**
수정 전: `TCP Rfl Pwr = -1` (1건) ↔ 수정 후: `TCP Rfl Pwr = 0` / 원본 `lam_etch_modify.csv`는 -1 그대로 보존

---

## 4장. 전처리 ② 식각 스텝 분리

**슬라이드 텍스트**
- Step 4(Al 주식각)와 Step 5(TiN·산화막 오버에치)는 공정 조건 자체가 다름 (BCl3 유량·압력 평균이 스텝마다 다름)
- 섞어서 학습시키면 '스텝이 달라서 나는 차이'와 '결함이라서 나는 차이'가 뒤섞임 → 스텝별 CSV로 분리
- Step 4: 6,025행 / Step 5: 6,804행 (합계 12,829 = 원본, 행 손실 없음)

**시각적 요소 — 순서 흐름**
`lam_etch_modify_2.csv` → `lam_etch_step4.csv` (6,025행) + `lam_etch_step5.csv` (6,804행) — 상수가 된 Step Number 열은 제거, wafer_id는 유지

---

## 5장. 분석 단위 문제 — 시점(행) 단위에는 신호가 없다

**슬라이드 텍스트**
- 행 단위로 표준화 후 로지스틱 회귀(일반)를 학습 → 시험용 Fail을 하나도 못 잡음 (재현율 0, F1 0)
- 원인 확인(16-5): 학습된 모델이 매긴 Fail 예측 확률이, 실제 Fail 행에서 오히려 더 낮게 나옴
- 라벨은 웨이퍼 단위인데 입력은 한 시점이라 신호 대 잡음비가 구조적으로 낮음

**시각적 요소 — 두 개 비교** (Step 4, 시험용 1,234행)
시험용 전체 행 평균 Fail 예측 확률 **0.1238** ↔ 실제 Fail 행(192개)만의 평균 **0.0554**

---

## 6장. 해결책 — 웨이퍼 단위로 압축

**슬라이드 텍스트**
- 한 웨이퍼의 여러 시점을 센서마다 평균 1개 + 표준편차 1개로 요약 (센서 19개 → 피처 38개)
- 1행 = 1웨이퍼가 되어 입력 단위가 라벨 단위(웨이퍼)와 일치, 학습/시험 분할도 단순해짐
- 결과 파일: `lam_etch_step4_agg.csv` 129행 / `lam_etch_step5_agg.csv` 128행 (각 41열)

**시각적 요소 — 순서 흐름**
웨이퍼당 원시 행 수십 개(step4 평균 46.7행) → groupby(wafer_id) → 센서별 mean + std → 웨이퍼당 1행(38피처)

---

## 7장. 결과 — 기준 모델과 나란히 비교

**슬라이드 텍스트**
- 정확도만 보면 기준 모델과 비슷해 보이지만, F1으로 보면 행 단위 모델은 0 → 웨이퍼 압축 후 0.33까지 개선
- 기준 모델(전부 Pass)은 정의상 Fail을 한 건도 못 잡음 — 정확도 0.84는 불균형이 만든 착시
- 시험용 Fail이 스텝당 4개뿐이라 압축 모델의 일반/balanced 순위는 우연에 크게 흔들릴 수 있음

**시각적 요소 — 표**

| 지표 | 기준 모델 | 행-로지스틱(일반) | 행-로지스틱(balanced) | 압축-로지스틱(일반) | 압축-로지스틱(balanced) |
|---|---|---|---|---|---|
| 평가 단위 / 개수 | — | 행 1,234 | 행 1,234 | 웨이퍼 26 | 웨이퍼 26 |
| 정확도 (Step 4) | 0.8444 | 0.8436 | 0.5259 | 0.8462 | 0.7692 |
| 정밀도 (Step 4) | — | 0.0 | 0.0398 | 0.5 | 0.25 |
| 재현율 (Step 4) | 0 | 0.0 | 0.0885 | 0.25 | 0.25 |
| F1 (Step 4) | 0 | 0.0 | 0.0549 | 0.3333 | 0.25 |
| F1 (Step 5) | 0 | 0.0 | 0.0767 | 0.0 | 0.2857 |

---

## 8장. 원인 후보 탐색 — 신호는 '평균'이 아니라 '산포'에 있다

**슬라이드 텍스트**
- 38개 피처 각각에 대해 (Fail 웨이퍼 평균 − Pass 웨이퍼 평균) ÷ 표준편차(효과크기)를 계산해 순위화
- 두 스텝 공통 상위: `RF Load_std`(둘 다 1위), `RF Tuner_std`, `Pressure_std`, `BCl3 Flow_mean`
- RF 매칭 네트워크(Load·Tuner)가 런 도중 얼마나 흔들렸는지가 Fail과 가장 크게 연관 — 단, 상관관계까지

**시각적 요소 — 두 개 비교** (Fail−Pass 효과크기 top 5)
Step 4: RF Load_std 0.8978 · Pressure_std 0.8827 · RF Phase Err_mean 0.7297 · BCl3 Flow_mean −0.6930 · RF Tuner_std 0.6657
Step 5: RF Load_std 0.7370 · RF Tuner_std 0.6394 · BCl3 Flow_mean 0.5412 · Pressure_std 0.5363 · RF Pwr_std −0.5158

---

## 9장. 3σ 관리도 — 정밀하지만 놓치는 게 많다

**슬라이드 텍스트**
- `RF Load_std`를 웨이퍼 순서로 늘어놓고 3σ 관리도 작성 — 중심선·관리한계는 Pass 웨이퍼만으로 산정
- Step 4: 관리상한 이탈 5개 중 실제 Fail 4/21, 오탐 1건 / Step 5: 이탈 3개 중 3/21, 오탐 0건
- 걸리면 거의 진짜(정밀도 높음)지만, 잡아내는 비율은 4/21·3/21로 낮아 대부분의 Fail은 선 아래에 있음

**시각적 요소 — 그림**
관리도 스케치: 초록 중심선(Pass 평균) + 빨간 점선 UCL, 파랑 점(Pass)·빨강 점(Fail), UCL 위에 빨강 4 + 파랑 1 (Step 4)

---

## 10장. 결론과 제안 전략

**슬라이드 텍스트**
- 시점이 아니라 웨이퍼 단위 요약값으로 봐야 한다 (데이터로 확인, 강하게 말할 수 있음)
- 원인 후보는 RF 매칭계·압력 제어계 쪽으로 좁혀짐 — 인과가 아닌 상관관계 수준
- 한계: Fail 웨이퍼 21개·시험용 4개, 교차검증 미실시 → 성능 수치는 이 분할에서 나온 값

**시각적 요소 — 순서 흐름** (3계층 이상탐지 전략)
① 실시간층(시점 급변 감시, 이번 범위 밖) → ② 런투런층(관리도 1차 경보 + 압축-로지스틱 보조) → ③ 드릴다운층(지목된 웨이퍼의 원본 시계열로 원인 확인)
